if [[ -n "$rvm_environments_path" && -s "$rvm_environments_path/ruby-1.9.2-p136@sweetsuite-auth" ]] ; then
  \. "$rvm_environments_path/ruby-1.9.2-p136@sweetsuite-auth"
else
  rvm --create  "ruby-1.9.2-p136@sweetsuite-auth"
fi
